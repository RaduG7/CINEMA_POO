<Window x:Class="CinemaUI.AdminLoggerPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaUI"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Username Label -->
        <TextBlock Text="Username:"
                       Grid.Row="0" Grid.Column="0"
                       Margin="5" FontSize="14" />

        <!-- Username TextBox -->
        <TextBox x:Name="username"
                     Grid.Row="1" Grid.Column="0"
                     Margin="5" Width="200" Height="20" />

        <!-- Password Label -->
        <TextBlock Text="Parola:"
                       Grid.Row="2" Grid.Column="0"
                       Margin="5" FontSize="14" />

        <!-- Password TextBox -->
        <PasswordBox x:Name="password"
                         Grid.Row="3" Grid.Column="0"
                         Margin="5" Width="200" Height="20" />

        <!-- Login Button -->
        <Button Content="Logare"
                    Grid.Row="4" Grid.Column="0"
                    Margin="10" Width="100" Height="30"
                    HorizontalAlignment="Right" Click="Login"/>
    </Grid>
</Window>
